# Application Environment
NODE_ENV=production
USE_MOCK_DB=true

# Server
PORT=5000

# Database (ignored when USE_MOCK_DB=true)
DB_HOST=localhost
DB_USER=root
DB_PASS=password
DB_NAME=uptrade
DB_PORT=3306

# CORS
CORS_ORIGIN=

# JWT Secret
JWT_SECRET=your-secret-key-here
